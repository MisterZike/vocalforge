#!/usr/bin/env bash
# VocalForge launcher - uses the local venv if present.
cd "$(dirname "$0")"
if [ -x .venv/bin/python ]; then PY=.venv/bin/python; else PY=python3; fi
exec "$PY" -m vocalforge.cli "$@"
